# CatalogoX — Blueprint

> Generated by The Architect on 2026-05-12
> Archetype: SaaS Web Application (Multi-tenant)

---

## 1. Project Overview

### Vision
CatalogoX es una plataforma SaaS para revendedores y emprendedores en México (LATAM después). Permite gestionar productos con costos y márgenes, generar catálogos digitales personalizados para compartir por WhatsApp, controlar ventas a crédito (fiado), y visualizar finanzas en un dashboard simple.

El usuario típico es un revendedor informal que vende 20-500 productos por redes sociales, lleva sus finanzas en Excel o de memoria, y necesita verse profesional ante sus clientes sin complicaciones técnicas.

### Goals
- Validar product-market fit con MVP funcional en México
- Convertir usuarios free a pro con límites estratégicos
- Ser la herramienta financiera + catálogo #1 para revendedores informales

### Success Metrics
- 500 usuarios registrados en primer mes
- 10% conversión free → pro
- NPS > 40 en usuarios activos

---

## 2. Tech Stack

| Layer | Technology | Why |
|-------|-----------|-----|
| Framework | Next.js 14 (App Router) | SSR para catálogos públicos SEO-friendly, Server Actions para mutations |
| Language | TypeScript (strict) | Type safety, mejor DX, menos bugs |
| Styling | Tailwind CSS v4 | Utility-first, rápido de iterar, estándar de la industria |
| Components | shadcn/ui | Componentes accesibles, customizables, copy-paste |
| Database | Supabase (PostgreSQL) | Auth + DB integrados, RLS para multi-tenancy, real-time disponible |
| ORM | Supabase Client (sin Prisma) | Evita problemas de connection pooling en Vercel serverless |
| Auth | Supabase Auth | Integrado con RLS, gratis, email + Google |
| Payments | MercadoPago | Mercado mexicano, Checkout Pro para suscripciones |
| Email | Resend | API simple, React Email para templates, buen free tier |
| Storage | Cloudflare R2 | S3-compatible, sin egress fees, barato |
| Hosting | Vercel | Zero-config para Next.js, previews, edge functions |
| Package Manager | pnpm | Rápido, eficiente en espacio |

---

## 3. Directory Structure

```
catalogox/
├── src/
│   ├── app/
│   │   ├── (marketing)/                    # Páginas públicas sin auth
│   │   │   ├── page.tsx                    # Landing page
│   │   │   ├── pricing/page.tsx            # Página de precios
│   │   │   └── layout.tsx                  # Layout marketing (navbar + footer)
│   │   ├── (app)/                          # App autenticada
│   │   │   ├── dashboard/page.tsx          # Dashboard financiero
│   │   │   ├── productos/
│   │   │   │   ├── page.tsx                # Lista de productos
│   │   │   │   ├── nuevo/page.tsx          # Crear producto
│   │   │   │   └── [id]/page.tsx           # Editar producto
│   │   │   ├── catalogos/
│   │   │   │   ├── page.tsx                # Lista de catálogos
│   │   │   │   ├── nuevo/page.tsx          # Crear catálogo
│   │   │   │   └── [id]/page.tsx           # Editar catálogo
│   │   │   ├── fiado/
│   │   │   │   ├── page.tsx                # Lista de clientes con fiado
│   │   │   │   ├── [clienteId]/page.tsx    # Detalle cliente + historial
│   │   │   │   └── nuevo/page.tsx          # Registrar venta a crédito
│   │   │   ├── configuracion/
│   │   │   │   ├── page.tsx                # Configuración general
│   │   │   │   ├── negocio/page.tsx        # Branding (logo, colores, nombre)
│   │   │   │   └── suscripcion/page.tsx    # Gestión de suscripción
│   │   │   └── layout.tsx                  # Layout app (sidebar + topbar)
│   │   ├── c/[slug]/                       # Catálogo público
│   │   │   ├── page.tsx                    # Vista catálogo con paginación
│   │   │   └── [productoId]/page.tsx       # Detalle producto (opcional)
│   │   ├── api/
│   │   │   ├── webhooks/
│   │   │   │   └── mercadopago/route.ts    # Webhook MercadoPago
│   │   │   ├── cron/
│   │   │   │   └── reporte-semanal/route.ts # Cron para emails semanales
│   │   │   └── upload/route.ts             # Upload a R2
│   │   ├── auth/
│   │   │   ├── login/page.tsx              # Login
│   │   │   ├── registro/page.tsx           # Registro
│   │   │   └── callback/route.ts           # OAuth callback
│   │   ├── layout.tsx                      # Root layout
│   │   └── globals.css                     # Tailwind + CSS variables
│   ├── components/
│   │   ├── ui/                             # shadcn/ui primitives
│   │   ├── marketing/                      # Landing page components
│   │   ├── app/                            # Dashboard components
│   │   │   ├── producto-card.tsx
│   │   │   ├── catalogo-preview.tsx
│   │   │   ├── fiado-table.tsx
│   │   │   ├── dashboard-stats.tsx
│   │   │   └── sidebar.tsx
│   │   ├── catalogo-publico/               # Catálogo público components
│   │   │   ├── perfil-negocio.tsx          # Mini perfil: logo, nombre, desc, redes, horario
│   │   │   ├── redes-sociales.tsx          # Links a Instagram, WhatsApp, Facebook
│   │   │   ├── producto-grid.tsx
│   │   │   ├── producto-card-publico.tsx
│   │   │   ├── catalogo-header.tsx
│   │   │   └── paginacion.tsx
│   │   └── shared/                         # Shared components
│   │       ├── logo.tsx
│   │       ├── image-upload.tsx
│   │       └── currency-input.tsx
│   ├── lib/
│   │   ├── supabase/
│   │   │   ├── client.ts                   # Browser client
│   │   │   ├── server.ts                   # Server client
│   │   │   └── middleware.ts               # Auth helper for middleware
│   │   ├── r2/
│   │   │   └── client.ts                   # Cloudflare R2 upload
│   │   ├── mercadopago/
│   │   │   ├── client.ts                   # MercadoPago SDK
│   │   │   └── helpers.ts                  # Checkout, subscription helpers
│   │   ├── email/
│   │   │   ├── client.ts                   # Resend client
│   │   │   └── templates/
│   │   │       ├── reporte-semanal.tsx     # React Email template
│   │   │       └── bienvenida.tsx
│   │   ├── utils.ts                        # cn(), formatCurrency(), etc.
│   │   └── constants.ts                    # Plan limits, config
│   ├── types/
│   │   ├── index.ts                        # Shared types
│   │   └── database.ts                     # Supabase generated types
│   └── middleware.ts                       # Auth + redirects
├── supabase/
│   ├── migrations/                         # SQL migrations
│   └── seed.sql                            # Seed data for dev
├── public/
│   ├── logo.svg
│   └── og-image.png
├── .env.local                              # Local env vars (gitignored)
├── .env.example                            # Example env vars
├── tailwind.config.ts
├── next.config.js
├── package.json
└── CLAUDE.md                               # Project instructions for Claude Code
```

---

## 4. Data Model

### Entities

**users** (managed by Supabase Auth, extended with profile)
| Field | Type | Notes |
|-------|------|-------|
| id | UUID | PK, from auth.users |
| email | TEXT | From auth |
| created_at | TIMESTAMPTZ | From auth |

**profiles**
| Field | Type | Notes |
|-------|------|-------|
| id | UUID | PK, FK → auth.users |
| nombre_negocio | TEXT | Nombre del negocio del revendedor |
| slug | TEXT | UNIQUE, para URL del catálogo |
| descripcion | TEXT | Descripción corta del negocio (max 280 chars) |
| logo_url | TEXT | URL en R2 |
| color_primario | TEXT | Hex color para branding |
| whatsapp | TEXT | Número de WhatsApp (con código país) |
| instagram | TEXT | Handle de Instagram (sin @) |
| facebook | TEXT | URL o handle de Facebook |
| horario | TEXT | Horario de atención (texto libre) |
| plan | TEXT | 'free' o 'pro' |
| mp_customer_id | TEXT | MercadoPago customer ID |
| mp_subscription_id | TEXT | MercadoPago subscription ID |
| created_at | TIMESTAMPTZ | DEFAULT now() |
| updated_at | TIMESTAMPTZ | DEFAULT now() |

**productos**
| Field | Type | Notes |
|-------|------|-------|
| id | UUID | PK |
| user_id | UUID | FK → profiles.id |
| nombre | TEXT | NOT NULL |
| descripcion | TEXT | |
| costo | DECIMAL(10,2) | Precio de compra |
| precio_menudeo | DECIMAL(10,2) | Precio venta menudeo |
| precio_mayoreo | DECIMAL(10,2) | Precio venta mayoreo (nullable) |
| stock | INTEGER | DEFAULT 0 |
| imagen_url | TEXT | URL en R2 |
| activo | BOOLEAN | DEFAULT true |
| created_at | TIMESTAMPTZ | DEFAULT now() |
| updated_at | TIMESTAMPTZ | DEFAULT now() |

**catalogos**
| Field | Type | Notes |
|-------|------|-------|
| id | UUID | PK |
| user_id | UUID | FK → profiles.id |
| nombre | TEXT | e.g., "Catálogo Menudeo", "Mayoreo VIP" |
| tipo_precio | TEXT | 'menudeo' o 'mayoreo' |
| slug | TEXT | Para URL, UNIQUE per user |
| activo | BOOLEAN | DEFAULT true |
| created_at | TIMESTAMPTZ | DEFAULT now() |

**catalogo_productos** (junction table)
| Field | Type | Notes |
|-------|------|-------|
| catalogo_id | UUID | FK → catalogos.id |
| producto_id | UUID | FK → productos.id |
| orden | INTEGER | Para ordenar productos |
| PRIMARY KEY | | (catalogo_id, producto_id) |

**clientes_fiado**
| Field | Type | Notes |
|-------|------|-------|
| id | UUID | PK |
| user_id | UUID | FK → profiles.id |
| nombre | TEXT | NOT NULL |
| telefono | TEXT | |
| notas | TEXT | |
| created_at | TIMESTAMPTZ | DEFAULT now() |

**ventas_fiado**
| Field | Type | Notes |
|-------|------|-------|
| id | UUID | PK |
| user_id | UUID | FK → profiles.id |
| cliente_id | UUID | FK → clientes_fiado.id |
| descripcion | TEXT | Qué compró |
| monto | DECIMAL(10,2) | Total de la venta |
| pagado | DECIMAL(10,2) | DEFAULT 0, cuánto ha pagado |
| fecha | DATE | Fecha de la venta |
| created_at | TIMESTAMPTZ | DEFAULT now() |

**pagos_fiado**
| Field | Type | Notes |
|-------|------|-------|
| id | UUID | PK |
| venta_id | UUID | FK → ventas_fiado.id |
| monto | DECIMAL(10,2) | Monto del abono |
| fecha | DATE | Fecha del pago |
| created_at | TIMESTAMPTZ | DEFAULT now() |

### Relationships
- profiles 1:N productos (un usuario tiene muchos productos)
- profiles 1:N catalogos (un usuario tiene muchos catálogos, free limitado a 1)
- catalogos N:M productos (via catalogo_productos)
- profiles 1:N clientes_fiado
- clientes_fiado 1:N ventas_fiado
- ventas_fiado 1:N pagos_fiado

### Database Schema (SQL)

```sql
-- Enable UUID extension
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Profiles table (extends auth.users)
CREATE TABLE profiles (
  id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  nombre_negocio TEXT,
  slug TEXT UNIQUE,
  descripcion TEXT,
  logo_url TEXT,
  color_primario TEXT DEFAULT '#1B4D3E',
  whatsapp TEXT,
  instagram TEXT,
  facebook TEXT,
  horario TEXT,
  plan TEXT DEFAULT 'free' CHECK (plan IN ('free', 'pro')),
  mp_customer_id TEXT,
  mp_subscription_id TEXT,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

-- Productos table
CREATE TABLE productos (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  nombre TEXT NOT NULL,
  descripcion TEXT,
  costo DECIMAL(10,2) DEFAULT 0,
  precio_menudeo DECIMAL(10,2) DEFAULT 0,
  precio_mayoreo DECIMAL(10,2),
  stock INTEGER DEFAULT 0,
  imagen_url TEXT,
  activo BOOLEAN DEFAULT true,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

-- Catalogos table
CREATE TABLE catalogos (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  nombre TEXT NOT NULL,
  tipo_precio TEXT NOT NULL CHECK (tipo_precio IN ('menudeo', 'mayoreo')),
  slug TEXT NOT NULL,
  activo BOOLEAN DEFAULT true,
  created_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE(user_id, slug)
);

-- Junction table for catalogo-productos
CREATE TABLE catalogo_productos (
  catalogo_id UUID REFERENCES catalogos(id) ON DELETE CASCADE,
  producto_id UUID REFERENCES productos(id) ON DELETE CASCADE,
  orden INTEGER DEFAULT 0,
  PRIMARY KEY (catalogo_id, producto_id)
);

-- Clientes fiado
CREATE TABLE clientes_fiado (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  nombre TEXT NOT NULL,
  telefono TEXT,
  notas TEXT,
  created_at TIMESTAMPTZ DEFAULT now()
);

-- Ventas a fiado
CREATE TABLE ventas_fiado (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  cliente_id UUID NOT NULL REFERENCES clientes_fiado(id) ON DELETE CASCADE,
  descripcion TEXT,
  monto DECIMAL(10,2) NOT NULL,
  pagado DECIMAL(10,2) DEFAULT 0,
  fecha DATE DEFAULT CURRENT_DATE,
  created_at TIMESTAMPTZ DEFAULT now()
);

-- Pagos de fiado
CREATE TABLE pagos_fiado (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  venta_id UUID NOT NULL REFERENCES ventas_fiado(id) ON DELETE CASCADE,
  monto DECIMAL(10,2) NOT NULL,
  fecha DATE DEFAULT CURRENT_DATE,
  created_at TIMESTAMPTZ DEFAULT now()
);

-- Indexes
CREATE INDEX idx_productos_user_id ON productos(user_id);
CREATE INDEX idx_catalogos_user_id ON catalogos(user_id);
CREATE INDEX idx_clientes_fiado_user_id ON clientes_fiado(user_id);
CREATE INDEX idx_ventas_fiado_cliente_id ON ventas_fiado(cliente_id);

-- Row Level Security
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE productos ENABLE ROW LEVEL SECURITY;
ALTER TABLE catalogos ENABLE ROW LEVEL SECURITY;
ALTER TABLE catalogo_productos ENABLE ROW LEVEL SECURITY;
ALTER TABLE clientes_fiado ENABLE ROW LEVEL SECURITY;
ALTER TABLE ventas_fiado ENABLE ROW LEVEL SECURITY;
ALTER TABLE pagos_fiado ENABLE ROW LEVEL SECURITY;

-- RLS Policies (users can only access their own data)
CREATE POLICY "Users can view own profile" ON profiles FOR SELECT USING (auth.uid() = id);
CREATE POLICY "Users can update own profile" ON profiles FOR UPDATE USING (auth.uid() = id);

CREATE POLICY "Users can CRUD own productos" ON productos FOR ALL USING (auth.uid() = user_id);
CREATE POLICY "Users can CRUD own catalogos" ON catalogos FOR ALL USING (auth.uid() = user_id);
CREATE POLICY "Users can CRUD own catalogo_productos" ON catalogo_productos FOR ALL 
  USING (catalogo_id IN (SELECT id FROM catalogos WHERE user_id = auth.uid()));

CREATE POLICY "Users can CRUD own clientes_fiado" ON clientes_fiado FOR ALL USING (auth.uid() = user_id);
CREATE POLICY "Users can CRUD own ventas_fiado" ON ventas_fiado FOR ALL USING (auth.uid() = user_id);
CREATE POLICY "Users can CRUD own pagos_fiado" ON pagos_fiado FOR ALL 
  USING (venta_id IN (SELECT id FROM ventas_fiado WHERE user_id = auth.uid()));

-- Public access for catalogo público (read-only, no auth needed)
CREATE POLICY "Public can view profiles with active catalogos" ON profiles FOR SELECT 
  USING (id IN (SELECT user_id FROM catalogos WHERE activo = true));
CREATE POLICY "Public can view active catalogos" ON catalogos FOR SELECT USING (activo = true);
CREATE POLICY "Public can view productos in active catalogos" ON productos FOR SELECT 
  USING (activo = true AND id IN (
    SELECT producto_id FROM catalogo_productos 
    WHERE catalogo_id IN (SELECT id FROM catalogos WHERE activo = true)
  ));
```

---

## 5. API Design

### Routes Overview

| Method | Path | Description | Auth |
|--------|------|-------------|------|
| GET | /api/productos | Listar productos del usuario | Yes |
| POST | /api/productos | Crear producto | Yes |
| PUT | /api/productos/[id] | Actualizar producto | Yes |
| DELETE | /api/productos/[id] | Eliminar producto | Yes |
| POST | /api/upload | Subir imagen a R2 | Yes |
| GET | /api/catalogos | Listar catálogos | Yes |
| POST | /api/catalogos | Crear catálogo | Yes |
| GET | /api/fiado/clientes | Listar clientes con fiado | Yes |
| POST | /api/fiado/ventas | Registrar venta a crédito | Yes |
| POST | /api/fiado/pagos | Registrar pago/abono | Yes |
| GET | /api/dashboard/stats | Estadísticas financieras | Yes |
| POST | /api/webhooks/mercadopago | Webhook de suscripciones | No (signature) |
| GET | /api/cron/reporte-semanal | Trigger reporte semanal | Cron secret |

### Key Endpoints Detail

**POST /api/productos**
```typescript
// Request
{
  nombre: string;           // required
  descripcion?: string;
  costo: number;
  precio_menudeo: number;
  precio_mayoreo?: number;
  stock?: number;
  imagen_url?: string;
}

// Response 201
{
  id: string;
  nombre: string;
  // ... all fields
  margen_menudeo: number;   // calculated: (precio_menudeo - costo) / costo * 100
  utilidad_menudeo: number; // calculated: precio_menudeo - costo
}

// Error 400: validation failed
// Error 403: limit reached (free plan, >10 productos)
```

**GET /api/dashboard/stats**
```typescript
// Query params: ?desde=2024-01-01&hasta=2024-01-31

// Response
{
  ingresos_totales: number;
  costos_totales: number;
  margen_promedio: number;
  utilidad_neta: number;
  ventas_fiado_pendientes: number;
  productos_activos: number;
  productos_bajo_stock: number; // stock < 5
}
```

**POST /api/webhooks/mercadopago**
```typescript
// MercadoPago sends subscription events
// Validate signature with MP secret
// Handle: subscription_authorized, subscription_cancelled, payment.created
// Update profiles.plan accordingly
```

---

## 6. Frontend Architecture

### Pages / Routes

| Route | Page | Description |
|-------|------|-------------|
| `/` | Landing | Hero, features, pricing CTA |
| `/pricing` | Pricing | Planes free vs pro, CTA a checkout |
| `/auth/login` | Login | Email/password + Google |
| `/auth/registro` | Registro | Sign up flow |
| `/dashboard` | Dashboard | Stats financieras, resumen |
| `/productos` | Productos | Lista con filtros, CRUD |
| `/productos/nuevo` | Nuevo producto | Form crear producto |
| `/productos/[id]` | Editar producto | Form editar |
| `/catalogos` | Catálogos | Lista de catálogos |
| `/catalogos/nuevo` | Nuevo catálogo | Seleccionar productos, tipo precio |
| `/fiado` | Fiado | Lista clientes con saldos |
| `/fiado/[clienteId]` | Cliente fiado | Historial, registrar pago |
| `/configuracion` | Config | Settings generales |
| `/configuracion/negocio` | Branding | Logo, nombre, descripción, colores, redes sociales, horario |
| `/configuracion/suscripcion` | Suscripción | Plan actual, upgrade/cancel |
| `/c/[slug]` | Catálogo público | Vista pública del catálogo |

### Component Hierarchy (Dashboard)

```
layout.tsx (AppLayout)
├── Sidebar
│   ├── Logo
│   ├── NavLinks
│   └── UserMenu
├── Topbar
│   └── MobileMenuToggle
└── children (page content)

dashboard/page.tsx
├── DashboardStats
│   ├── StatCard (Ingresos)
│   ├── StatCard (Utilidad)
│   ├── StatCard (Margen)
│   └── StatCard (Fiado pendiente)
├── VentasRecientes (table)
└── ProductosBajoStock (alert list)
```

### Component Hierarchy (Catálogo Público)

```
c/[slug]/page.tsx
├── PerfilNegocio
│   ├── Logo (del negocio)
│   ├── NombreNegocio
│   ├── Descripcion (max 280 chars)
│   ├── RedesSociales
│   │   ├── WhatsAppButton (abre chat directo)
│   │   ├── InstagramLink
│   │   └── FacebookLink
│   └── Horario
├── CatalogoHeader
│   └── TipoPrecio badge + filtros (opcional)
├── ProductoGrid
│   └── ProductoCardPublico[]
│       ├── Imagen
│       ├── Nombre
│       └── Precio
├── Paginacion
└── Footer
    └── MarcaAgua ("Hecho con CatalogoX") -- solo plan free
```

### State Management
- **Server Components by default** — fetch data directly from Supabase
- **Client Components** only for interactivity (forms, modals, filters)
- **Server Actions** for mutations (create, update, delete)
- **URL state** for filters and pagination (searchParams)
- **No global state library** — overkill for MVP

---

## 7. Design System

### Colors

| Role | Hex | Usage |
|------|-----|-------|
| Primary | `#1B4D3E` | CTAs, links, accent |
| Primary Light | `#2D6A4F` | Hover states |
| Primary Foreground | `#FFFFFF` | Text on primary bg |
| Background | `#FFFFFF` | Page background |
| Surface | `#F9FAFB` | Cards, sidebar, inputs |
| Border | `#E5E7EB` | Borders, dividers |
| Text | `#111827` | Primary text |
| Text Muted | `#6B7280` | Secondary text |
| Success | `#059669` | Confirmations, profit |
| Danger | `#DC2626` | Errors, debt |
| Warning | `#D97706` | Alerts, low stock |

### Typography

| Role | Font | Size | Weight |
|------|------|------|--------|
| H1 | Inter | 32px | 600 |
| H2 | Inter | 24px | 600 |
| H3 | Inter | 20px | 600 |
| Body | Inter | 16px | 400 |
| Small | Inter | 14px | 400 |
| Label | Inter | 14px | 500 |
| Mono (prices) | JetBrains Mono | 16px | 500 |

### Spacing & Layout
- **Spacing scale:** 4px base (4, 8, 12, 16, 24, 32, 48, 64)
- **Border radius:** 6px (buttons, inputs), 8px (cards), 12px (modals)
- **Max content width:** 1280px
- **Sidebar width:** 256px
- **Breakpoints:** sm: 640px, md: 768px, lg: 1024px, xl: 1280px

### Component Style
- Clean, minimal aesthetic
- Subtle shadows (`shadow-sm`) on cards
- Rounded corners (not sharp, not pill)
- Hover states with slight background change
- Focus rings in primary color
- No heavy animations — simple transitions (150ms)

---

## 8. Authentication & Authorization

### Auth Flow
1. User lands on landing page → CTA "Empieza gratis"
2. Registro con email/password o Google
3. Supabase creates user in auth.users
4. Trigger creates profile row with default values
5. Redirect to /dashboard (onboarding inline, no separate flow for MVP)

### Protected Routes
- **Public:** `/`, `/pricing`, `/auth/*`, `/c/[slug]`
- **Protected:** `/dashboard`, `/productos`, `/catalogos`, `/fiado`, `/configuracion`

### Roles & Permissions
| Role | Can Do |
|------|--------|
| free | CRUD hasta 10 productos, 1 catálogo, sin fiado, sin reportes email |
| pro | Todo desbloqueado |

Plan enforcement happens at API level — check `profiles.plan` before allowing restricted actions.

### Session Management
- Supabase handles sessions via cookies
- `middleware.ts` refreshes session on every request
- Server components get session via `createServerClient`
- Client components get session via `createBrowserClient`

---

## 9. Build Order

**Step 1: Project Scaffolding**
```bash
pnpm create next-app@latest catalogox --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
cd catalogox
pnpm add @supabase/supabase-js @supabase/ssr
pnpm dlx shadcn@latest init
```
- Configure `tailwind.config.ts` with custom colors
- Set up path aliases in `tsconfig.json`
- Create `.env.local` with Supabase keys
- Init shadcn/ui with custom theme

**Step 2: Database Schema + Supabase Setup**
- Create Supabase project
- Run SQL migrations (from Section 4)
- Enable Row Level Security
- Generate TypeScript types: `pnpm supabase gen types typescript`
- Set up `lib/supabase/client.ts` and `lib/supabase/server.ts`

**Step 3: Authentication**
- Create `middleware.ts` for session refresh
- Build `/auth/login` and `/auth/registro` pages
- Configure Google OAuth in Supabase dashboard
- Create auth callback route
- Test full auth flow

**Step 4: App Layout + Navigation**
- Build `(app)/layout.tsx` with Sidebar
- Create Sidebar component with nav links
- Add user menu with logout
- Responsive mobile menu

**Step 5: CRUD Productos**
- `/productos` — list with search/filter
- `/productos/nuevo` — create form with image upload
- `/productos/[id]` — edit form
- Server Actions for create/update/delete
- Display calculated margin and profit per product

**Step 6: Image Upload (R2)**
- Set up Cloudflare R2 bucket
- Create `/api/upload` route
- Build `ImageUpload` component with preview
- Connect to producto forms

**Step 7: Catálogos**
- `/catalogos` — list user's catalogos
- `/catalogos/nuevo` — select productos, set tipo_precio, generate slug
- Enforce free plan limit (1 catálogo)

**Step 8: Catálogo Público**
- `/c/[slug]` — public page, no auth required
- Fetch catálogo + productos + profile data with RLS bypass for public
- **Perfil del negocio arriba**: logo, nombre, descripción, redes sociales (WhatsApp con link directo, Instagram, Facebook), horario
- Apply user's branding (color primario en header/botones)
- Pagination (12 productos por página)
- "Hecho con CatalogoX" watermark for free plan

**Step 9: Dashboard Financiero**
- `/dashboard` — stats overview
- Calculate: ingresos, costos, margen, utilidad
- Date range filter (este mes, último mes, custom)
- Productos bajo stock alert

**Step 10: Sistema de Fiado**
- `/fiado` — lista clientes con saldo pendiente
- `/fiado/[clienteId]` — historial de ventas y pagos
- Forms para registrar venta a crédito y abonos
- Cálculo automático de saldo pendiente
- Enforce: only pro plan

**Step 11: MercadoPago Suscripciones**
- Create MercadoPago app and get credentials
- Build checkout flow for pro plan
- Create `/api/webhooks/mercadopago` webhook handler
- Handle subscription events (authorized, cancelled)
- Update `profiles.plan` accordingly
- `/configuracion/suscripcion` — manage subscription

**Step 12: Reportes Semanales por Email**
- Set up Resend account
- Create React Email template for weekly report
- Build `/api/cron/reporte-semanal` endpoint
- Configure Vercel Cron (domingos 9am)
- Only send to pro users

**Step 13: Landing Page + Pricing**
- `/` — hero, features, testimonials, CTA
- `/pricing` — free vs pro comparison table
- SEO metadata, OG image

**Step 14: Configuración Negocio (Branding + Perfil)**
- `/configuracion/negocio` — logo upload, nombre, descripción corta (280 chars max), color primario
- Redes sociales: WhatsApp (número), Instagram (handle), Facebook (URL)
- Horario de atención (texto libre, e.g., "Lun-Vie 9am-6pm")
- Preview how catálogo público will look with all profile data

**Step 15: Polish + Deploy**
- Loading states (Suspense, skeletons)
- Error boundaries
- Empty states
- Mobile responsive pass
- Vercel deployment
- Custom domain setup
- Environment variables in Vercel

---

## 10. Environment Setup

### Prerequisites
- Node.js 18+
- pnpm 8+
- Supabase account
- Cloudflare account (for R2)
- MercadoPago developer account
- Resend account
- Vercel account

### Environment Variables

| Variable | Description | Where to Get |
|----------|-------------|--------------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase project URL | Supabase dashboard → Settings → API |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase anon key | Supabase dashboard → Settings → API |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase service key (server only) | Supabase dashboard → Settings → API |
| `R2_ACCOUNT_ID` | Cloudflare account ID | Cloudflare dashboard |
| `R2_ACCESS_KEY_ID` | R2 access key | Cloudflare R2 → Manage API Tokens |
| `R2_SECRET_ACCESS_KEY` | R2 secret key | Cloudflare R2 → Manage API Tokens |
| `R2_BUCKET_NAME` | R2 bucket name | e.g., "catalogox-images" |
| `R2_PUBLIC_URL` | R2 public URL | Cloudflare R2 → bucket → Settings |
| `MP_ACCESS_TOKEN` | MercadoPago access token | MercadoPago developers → Credentials |
| `MP_WEBHOOK_SECRET` | MercadoPago webhook secret | MercadoPago developers → Webhooks |
| `RESEND_API_KEY` | Resend API key | Resend dashboard → API Keys |
| `CRON_SECRET` | Secret for cron endpoints | Generate random string |

### Initial Setup Commands

```bash
# Clone and install
git clone <repo>
cd catalogox
pnpm install

# Set up environment
cp .env.example .env.local
# Fill in all values

# Generate Supabase types
pnpm supabase gen types typescript --project-id <project-id> > src/types/database.ts

# Run dev server
pnpm dev
```

---

## 11. Dependencies

### Core
| Package | Purpose |
|---------|---------|
| next | Framework |
| react, react-dom | UI library |
| typescript | Type safety |
| @supabase/supabase-js | Supabase client |
| @supabase/ssr | Supabase SSR helpers |
| tailwindcss | Styling |
| @aws-sdk/client-s3 | R2 upload (S3-compatible) |
| mercadopago | MercadoPago SDK |
| resend | Email sending |
| @react-email/components | Email templates |
| zod | Validation |
| date-fns | Date formatting |
| clsx, tailwind-merge | Class utilities |

### Dev
| Package | Purpose |
|---------|---------|
| @types/node, @types/react | TypeScript types |
| eslint, eslint-config-next | Linting |
| prettier, prettier-plugin-tailwindcss | Formatting |
| supabase | CLI for types generation |

---

## 12. Deployment Strategy

### Hosting
Vercel — zero-config for Next.js, automatic previews on PRs, edge functions for middleware.

### CI/CD
- Push to `main` → auto deploy to production
- Push to PR → preview deployment
- No staging environment for MVP (preview deploys are sufficient)

### Domain & DNS
- Primary: `catalogox.com` (or `.mx`)
- Add domain in Vercel dashboard
- Configure DNS records as instructed

### Environments
| Env | URL | Database | Payments |
|-----|-----|----------|----------|
| Development | localhost:3000 | Supabase (same project, different branch if needed) | MercadoPago sandbox |
| Preview | *.vercel.app | Supabase prod | MercadoPago sandbox |
| Production | catalogox.com | Supabase prod | MercadoPago prod |

---

## 13. Testing Strategy

### For MVP
Testing is minimal — focus on shipping. Add tests in v2.

### Unit Tests (v2)
- Utility functions (margin calculations, currency formatting)
- Framework: Vitest

### Integration Tests (v2)
- API routes with mocked Supabase
- Framework: Vitest + MSW

### E2E Tests (v2)
- Critical flows: signup, create product, create catalog, view public catalog
- Framework: Playwright

---

## 14. Skills to Use During Build

| Skill | When to Use | Why |
|-------|-------------|-----|
| `/shadcn-ui` | Step 1 (scaffolding) | Set up and customize shadcn components |
| `/frontend-design` | Steps 4, 5, 8, 13 (layouts, productos UI, catálogo público, landing) | Production-grade, distinctive UI |
| `/seo-audit` | Step 15 (polish) | Audit landing page and catálogo público before launch |

---

## 15. CLAUDE.md for Target Project

```markdown
# CatalogoX

SaaS multi-tenant para revendedores en México. Gestión de productos, catálogos digitales, control de fiado, y dashboard financiero.

## Commands

- `pnpm dev` — Start development server
- `pnpm build` — Production build
- `pnpm lint` — Run ESLint
- `pnpm supabase gen types typescript --project-id <id> > src/types/database.ts` — Regenerate DB types

## Tech Stack

Next.js 14 (App Router) + TypeScript + Tailwind CSS + shadcn/ui + Supabase (Auth + DB) + Cloudflare R2 + MercadoPago + Resend + Vercel

## Architecture

### Directory Structure
- `src/app/(marketing)/` — Public pages (landing, pricing)
- `src/app/(app)/` — Authenticated app (dashboard, productos, catalogos, fiado)
- `src/app/c/[slug]/` — Public catalog pages
- `src/app/api/` — API routes and webhooks
- `src/components/` — UI components (ui/, app/, marketing/, catalogo-publico/)
- `src/lib/` — Utilities (supabase/, r2/, mercadopago/, email/)
- `src/types/` — TypeScript types
- `supabase/` — Migrations and seed data

### Data Flow
- Server Components fetch data directly from Supabase
- Mutations via Server Actions
- RLS enforces data isolation per user
- Public catalogs bypass auth via RLS policy
- `/c/[slug]` shows business profile (logo, name, description, social links, schedule) + product grid

### Key Patterns
- **Server Components by default** — only "use client" when interactivity is needed
- **Supabase client directly** — NO Prisma (connection pooling issues on Vercel)
- **URL state for filters** — use searchParams, not useState
- **Plan enforcement at API level** — check profiles.plan before restricted actions

## Code Organization Rules

1. **One component per file.** Max 300 lines.
2. **Path alias:** Use `@/` for `src/` imports.
3. **No barrel exports.** Import directly from source.
4. **Colocate page-specific components** next to their page.
5. **Supabase queries in lib/ or Server Components** — never in client components.

## Design System

### Colors
- Primary: `#1B4D3E` (verde bosque)
- Primary Light: `#2D6A4F`
- Background: `#FFFFFF`
- Surface: `#F9FAFB`
- Border: `#E5E7EB`
- Text: `#111827`
- Text Muted: `#6B7280`
- Success: `#059669`
- Danger: `#DC2626`
- Warning: `#D97706`

### Typography
- Headings: Inter, 600 weight
- Body: Inter, 400 weight, 16px
- Prices/numbers: JetBrains Mono, 500 weight

### Style
- Border radius: 6px buttons, 8px cards
- Shadows: shadow-sm on cards
- Spacing base: 4px
- Aesthetic: Clean, minimal, professional but warm

## Environment Variables

| Variable | Description |
|----------|-------------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase project URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase anon key |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase service key |
| `R2_ACCOUNT_ID` | Cloudflare account ID |
| `R2_ACCESS_KEY_ID` | R2 access key |
| `R2_SECRET_ACCESS_KEY` | R2 secret |
| `R2_BUCKET_NAME` | R2 bucket |
| `R2_PUBLIC_URL` | R2 public URL |
| `MP_ACCESS_TOKEN` | MercadoPago access token |
| `MP_WEBHOOK_SECRET` | MercadoPago webhook secret |
| `RESEND_API_KEY` | Resend API key |
| `CRON_SECRET` | Secret for cron endpoints |

## Reglas No Negociables

1. **TypeScript strict mode** — no `any` types, fix all type errors
2. **Use Supabase client directly** — do NOT install Prisma
3. **RLS must be enabled** on all tables — never bypass in app code
4. **Plan limits enforced server-side** — free: 10 productos, 1 catálogo, no fiado
5. **All prices in MXN** — format with `$` and 2 decimals
6. **Mobile-first responsive** — test every page on mobile viewport
7. **Paginación en catálogo público** — no scroll infinito
8. **Marca de agua "Hecho con CatalogoX"** en catálogos de plan free
9. **Catálogo público muestra perfil del negocio** — logo, nombre, descripción, redes (WhatsApp, Instagram, Facebook), horario — arriba del grid de productos
```

---

## 16. Reglas No Negociables

1. **TypeScript strict mode, no `any` types** — every variable and function must be typed
2. **Use Supabase client directly** — do NOT use Prisma to avoid connection pooling issues on Vercel serverless
3. **RLS enabled on ALL tables** — data isolation is enforced at database level, never trust app code alone
4. **Plan limits enforced server-side** — always check `profiles.plan` in API routes/Server Actions before allowing restricted features
5. **All monetary values in DECIMAL(10,2)** — never use FLOAT for money
6. **All prices displayed in MXN format** — `$1,234.56` with peso sign and proper thousands separator
7. **Mobile-first responsive design** — every page must work on 375px viewport
8. **Catálogo público uses simple pagination** — 12 products per page, no infinite scroll
9. **Watermark "Hecho con CatalogoX"** on free plan catalogs — links to landing page
10. **Catálogo público muestra perfil del negocio arriba** — logo, nombre, descripción, redes sociales (WhatsApp con link directo, Instagram, Facebook), horario de atención
11. **Never commit `.env.local`** — use `.env.example` as reference
